# FlowMind 完整 HTML 原型

直接打开 `index.html` 即可使用。原型使用原生 HTML、CSS 和 JavaScript，不需要安装依赖。

主要交互：

- 左侧 18 个模块完整切换。
- 项目详情、需求录入、PRD、技术方案、任务拆分、研发执行、审查、测试、运维等页面。
- 新建需求、生成 PRD、审批、驳回、任务状态切换、Agent 暂停/继续、测试运行、发布确认。
- 搜索、通知抽屉、命令面板、页面标签切换、表格筛选、详情抽屉、Toast 提示。
- 模拟数据保存在浏览器 `localStorage`，刷新后仍可保留部分交互状态。
